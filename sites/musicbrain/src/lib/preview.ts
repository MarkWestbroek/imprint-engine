/**
 * The as-of preview lives in the engine (@imprint/runtime-admin, preview.ts);
 * this module only keeps the site's import path.
 */
export { ASOF_COOKIE, getPreview, readOpts, type Preview } from "@imprint/runtime-admin";
