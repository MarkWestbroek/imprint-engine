# Imprint product-site database (Postgres, npm run db:up). Keep this separate from MusicBrain.
DATABASE_URL=postgres://imprint:imprint-dev@localhost:5434/imprint
# Signs the admin session cookie; any long random string. Without it nobody can sign in.
SESSION_SECRET=change-me
